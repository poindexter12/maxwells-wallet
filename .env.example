# Docker build configuration
# Copy this file to .env and modify as needed

# Enable pseudo locale for i18n QA testing (default: false)
# Set to true to include the pseudo locale option in the language selector
ENABLE_PSEUDO=false
