# direnv configuration
# Automatically loads .env and activates virtual environment

# Load environment variables from .env file
dotenv_if_exists .env

# Activate uv-managed virtual environment
layout python-uv
