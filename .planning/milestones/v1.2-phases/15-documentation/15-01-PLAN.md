---
phase: 15-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - QUICKSTART.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "CLAUDE.md Development Commands section references only just recipes, zero make commands"
    - "CLAUDE.md has no 'Direct Commands (When Necessary)' section"
    - "CLAUDE.md positions mise as THE single prerequisite with inline install snippet"
    - "README.md setup/dev instructions use just commands exclusively"
    - "README.md includes mise curl one-liner as the only prerequisite"
    - "QUICKSTART.md uses just commands exclusively"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Canonical just command reference for agents"
      contains: "just setup"
    - path: "README.md"
      provides: "User-facing onboarding with mise + just"
      contains: "curl https://mise.run"
    - path: "QUICKSTART.md"
      provides: "Quick reference with just commands"
      contains: "just"
  key_links:
    - from: "CLAUDE.md"
      to: "justfile"
      via: "recipe names match actual just modules"
      pattern: "just (dev|db|test|docker|i18n|release|utils)::"
    - from: "README.md"
      to: ".mise.toml"
      via: "mise install snippet references mise as prerequisite"
      pattern: "curl https://mise.run"
---

<objective>
Rewrite the three primary documentation files (CLAUDE.md, README.md, QUICKSTART.md) to reference just recipes exclusively, with mise as the single prerequisite.

Purpose: These are the anchor documents that all other docs and agent files reference. Getting them right first establishes the canonical command syntax (e.g., `just dev::dev`, `just test::backend`) that Plan 02 will propagate across all remaining files.

Output: Updated CLAUDE.md with comprehensive just command reference, updated README.md with mise-first onboarding, updated QUICKSTART.md with just commands.
</objective>

<execution_context>
@/Users/joe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-documentation/15-CONTEXT.md
@.planning/phases/15-documentation/15-RESEARCH.md

@CLAUDE.md
@README.md
@QUICKSTART.md
@justfile
@.just/dev.just
@.just/db.just
@.just/test.just
@.just/docker.just
@.just/i18n.just
@.just/release.just
@.just/utils.just
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md Development Commands and related sections</name>
  <files>CLAUDE.md</files>
  <action>
Rewrite the following sections of CLAUDE.md:

**1. Development Environment section:**
- Keep the VS Code Devcontainer subsection (it's useful existing context — discretion: keep it, simplify slightly)
- Replace the "Local Setup (Alternative)" subsection with a mise-first prerequisite block:
  - mise is THE single prerequisite
  - Include inline: `curl https://mise.run | sh`
  - State that mise auto-installs Node, Python, uv, just, and gum
  - No manual fallback path (no "alternatively install Node + Python + uv manually")

**2. Development Commands section — full replacement:**
- Replace the "IMPORTANT: Always prefer `make` commands" callout with a STRONGER version:
  `**IMPORTANT**: Always use `just` recipes. The justfile handles environment setup, paths, and provides enhanced terminal UX via gum. Run bare `just` (no args) to see all available recipes. Do not use direct npm/uv/alembic commands for tasks that have just recipes.`
- Remove the "### Using Make (Preferred)" heading entirely
- Remove the "### Direct Commands (When Necessary)" subsection entirely (per locked decision)
- Organize commands by domain using exact recipe names from the justfile modules:

Root recipes (no module prefix):
- `just setup` — First-time setup (install deps + init database)
- `just install` — Install all dependencies
- `just install-backend` — Install backend deps only
- `just install-frontend` — Install frontend deps only

Development (`dev::` module):
- `just dev::dev` — Run both backend and frontend in parallel
- `just dev::backend` — Run backend server only
- `just dev::frontend` — Run frontend server only
- `just dev::build-frontend` — Build frontend for production

Database (`db::` module):
- `just db::init` — Initialize database
- `just db::seed` — Seed with sample data
- `just db::reset` — Reset database (DESTRUCTIVE — asks for confirmation)
- `just db::migrate MESSAGE="description"` — Create new migration
- `just db::upgrade` — Apply migrations
- `just db::demo-setup` — Set up demo data

Testing & Quality (`test::` module):
- `just test::backend` — Run backend tests
- `just test::coverage` — Run tests with coverage report
- `just test::e2e` — Run E2E tests (requires servers running)
- `just test::e2e-install` — Install Playwright browsers (one-time)
- `just test::chaos` — Run chaos/monkey tests
- `just test::lint` — Lint all code
- `just test::quality` — Run all quality checks (lint + typecheck + vulture)
- `just test::typecheck` — Type checking with mypy
- `just test::vulture` — Dead code detection
- `just test::security-audit` — Security audit

Docker (`docker::` module):
- `just docker::build` — Build Docker image
- `just docker::up` — Start containers
- `just docker::down` — Stop containers
- `just docker::logs` — View container logs
- `just docker::shell` — Shell into running container
- `just docker::clean` — Remove containers and volumes

Internationalization (`i18n::` module):
- `just i18n::upload` — Push source strings to Crowdin
- `just i18n::download` — Pull translations from Crowdin
- `just i18n::status` — Check translation progress
- `just i18n::pseudo` — Generate pseudo-locale for testing

Release (`release::` module):
- `just release::release VERSION="x.y.z"` — Create release
- `just release::check` — Pre-flight validation
- `just release::validate` — Validation (fails on errors)

Utilities (`utils::` module):
- `just utils::status` — Check server status
- `just utils::info` — Show project info
- `just utils::clean` — Clean build artifacts
- `just utils::clean-all` — Clean everything (DESTRUCTIVE)
- `just utils::check-deps` — Verify dependencies installed

**3. Architecture Notes section:**
- In the "Backend" subsection, if it mentions `uv run uvicorn` or `uv run alembic` as direct commands, remove those references or replace with the just recipe equivalents
- In "Python Environment" subsection, keep the uv/direnv notes but remove any "run commands directly" framing

**4. Typical Workflow section:**
- Replace `make` targets with just equivalents
- Replace step 1 with: "Use `just` recipes for setup, dev, tests, and migrations (see commands above)."
- Replace step 2 with: "Run `just dev::dev` to start both servers for local development."

**5. Testing Guidelines section (if it contains make references):**
- Replace `make test-backend` with `just test::backend`
- Replace `make test-e2e` with `just test::e2e`
- Replace `make test-frontend` with `just test::lint-frontend` or appropriate just recipe
- Update any other make references

**6. Internationalization section:**
- Replace make commands table with just equivalents:
  - `make translate-upload` → `just i18n::upload`
  - `make translate-download` → `just i18n::download`
  - `make translate-status` → `just i18n::status`
  - `make translate-pseudo` → `just i18n::pseudo`

**Clean break posture:** Write everything as if just was always the tool. No "migrated from make" notes. No mention of make as a tool anywhere.
  </action>
  <verify>
    <automated>cd /Users/joe/Code/github.com/poindexter12/maxwells-wallet && ! git grep -i '`make' -- CLAUDE.md | grep -v '.planning/' | grep -v 'Makefile'</automated>
  </verify>
  <done>CLAUDE.md contains zero backtick-wrapped make commands. Development Commands section is organized by just module with accurate recipe names. "Direct Commands" section is gone. mise is framed as THE single prerequisite with curl one-liner.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md and QUICKSTART.md with mise-first onboarding</name>
  <files>README.md, QUICKSTART.md</files>
  <action>
**README.md updates:**

1. **Prerequisites/Requirements section:**
   - Replace any "Requirements: Node.js 22+, Python 3.11+, uv" with mise-first framing
   - Add inline mise install snippet: `curl https://mise.run | sh`
   - State: "mise auto-installs all dev tools (Node, Python, uv, just, gum) when you enter the project directory."
   - No manual fallback path (per locked decision)
   - mise + just appear in setup/development sections only, NOT in project intro or tech stack bullet points

2. **Setup/Getting Started section:**
   - Replace `make setup` with `just setup`
   - Replace `make dev` with `just dev::dev`
   - Replace `make help` with bare `just` (no args)

3. **Development section:**
   - Replace all `make <target>` with equivalent `just` recipes
   - Keep it concise (README is user-facing brief, CLAUDE.md is comprehensive)
   - Key recipes to show: `just setup`, `just dev::dev`, `just test::backend`, `just db::reset`

4. **Testing section (if present):**
   - Replace `make test-backend` → `just test::backend`
   - Replace `make test-e2e` → `just test::e2e`

5. **Docker section (if present):**
   - Replace `make docker-build` → `just docker::build`
   - Replace `make docker-up` → `just docker::up`

6. **Any other make references:**
   - Find and replace all remaining make commands with just equivalents

**QUICKSTART.md updates:**

1. Replace all `make` commands with `just` equivalents
2. Add mise prerequisite if not present
3. Keep the quick/concise tone — this is a fast-start guide

**Mapping reference for both files (verify against actual justfile):**
- `make setup` → `just setup`
- `make dev` → `just dev::dev`
- `make backend` → `just dev::backend`
- `make frontend` → `just dev::frontend`
- `make build-frontend` → `just dev::build-frontend`
- `make test-backend` → `just test::backend`
- `make test-all` → `just test::all`
- `make test-e2e` → `just test::e2e`
- `make lint` → `just test::lint`
- `make quality` → `just test::quality`
- `make db-init` → `just db::init`
- `make db-seed` → `just db::seed`
- `make db-reset` → `just db::reset`
- `make db-migrate` → `just db::migrate MESSAGE="description"`
- `make db-upgrade` → `just db::upgrade`
- `make docker-build` → `just docker::build`
- `make docker-up` → `just docker::up`
- `make docker-down` → `just docker::down`
- `make help` → `just` (bare, no args)

**Clean break posture:** No mention of make. Docs read as if just was always the tool.
  </action>
  <verify>
    <automated>cd /Users/joe/Code/github.com/poindexter12/maxwells-wallet && ! git grep -i '`make' -- README.md QUICKSTART.md | grep -v '.planning/' | grep -v 'Makefile'</automated>
  </verify>
  <done>README.md and QUICKSTART.md contain zero backtick-wrapped make commands. README has mise curl one-liner as the only prerequisite. No manual tool installation paths. No mention of make.</done>
</task>

</tasks>

<verification>
Run the following to verify Plan 01 success:
```bash
# No make references in primary docs
git grep -i '`make' -- CLAUDE.md README.md QUICKSTART.md

# Verify just recipes are referenced (spot check)
grep -c 'just ' CLAUDE.md   # Should be 30+
grep -c 'just ' README.md   # Should be 5+
grep 'curl https://mise.run' README.md  # Should match
grep 'curl https://mise.run' CLAUDE.md  # Should match
```
</verification>

<success_criteria>
- CLAUDE.md Development Commands uses only just recipes organized by module
- CLAUDE.md has no "Direct Commands" section
- CLAUDE.md has mise prerequisite with curl one-liner
- README.md has mise-first onboarding with no manual tool installation
- README.md uses just commands exclusively
- QUICKSTART.md uses just commands exclusively
- Zero `make` command references in all three files (excluding historical context)
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation/15-01-SUMMARY.md`
</output>
