---
phase: 15-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - .claude/agents/techlead.mdc
  - .claude/agents/backend-lead.mdc
  - .claude/agents/frontend-lead.mdc
  - .claude/agents/db-lead.mdc
  - .claude/agents/testlead.mdc
  - .claude/agents/i18n-lead.mdc
  - .claude/skills/fastapi.mdc
  - .claude/skills/postgres.mdc
  - .claude/skills/testing-backend.mdc
  - CONTRIBUTING.md
  - OPENSSF.md
  - docs/MAKEFILE.md
  - docs/i18n-workflow.md
  - docs/installation.md
  - docs/requirements/README.md
  - docs/requirements/TECHNICAL_SPECIFICATIONS.md
  - docs/tech-debt/README.md
  - docs/tech-debt/backend.md
  - docs/tech-debt/database.md
  - docs/tech-debt/testing.md
  - docs-site/developer/architecture.md
  - docs-site/developer/database.md
  - docs-site/developer/testing.md
  - docs-site/getting-started/development.md
  - docs-site/getting-started/quickstart.md
  - docs-site/index.md
  - frontend/e2e/README.md
  - frontend/src/messages/CLAUDE.md
  - backend/README.md
  - data/CLAUDE.md
  - deploy/swag-test/README.md
  - .claude/scratchpad/sqlalchemy-migration-plan.md
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "git grep -i '`make' returns zero results (excluding Makefile, make/, .planning/)"
    - "All .claude/agents/*.mdc files reference just recipes instead of make targets"
    - "All .claude/skills/*.mdc files reference just recipes instead of make targets"
    - "docs/MAKEFILE.md is rewritten as a just recipe reference (or deleted)"
    - "docs/i18n-workflow.md references just i18n:: recipes"
    - "frontend/e2e/README.md references just test::e2e"
    - "CHANGELOG.md is left untouched (historical record)"
  artifacts:
    - path: ".claude/agents/techlead.mdc"
      provides: "Agent guidance referencing just"
      contains: "just"
    - path: "docs/i18n-workflow.md"
      provides: "i18n workflow with just commands"
      contains: "just i18n::"
    - path: "frontend/e2e/README.md"
      provides: "E2E test docs with just commands"
      contains: "just test::e2e"
  key_links:
    - from: ".claude/agents/*.mdc"
      to: "CLAUDE.md"
      via: "agents reference CLAUDE.md commands"
      pattern: "just"
    - from: "docs/i18n-workflow.md"
      to: ".just/i18n.just"
      via: "documented commands match actual recipes"
      pattern: "just i18n::"
---

<objective>
Sweep all remaining documentation files in the repo to replace make references with just equivalents, then run the final grep audit to verify DOC-03 (zero remaining make references).

Purpose: Plan 01 established the canonical command reference in CLAUDE.md and README.md. This plan propagates those changes to all agent files, skill cards, docs, READMEs, and other scattered files. The final grep audit is the DOC-03 acceptance gate.

Output: All documentation files updated, grep audit passes with zero results.
</objective>

<execution_context>
@/Users/joe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-documentation/15-CONTEXT.md
@.planning/phases/15-documentation/15-RESEARCH.md
@.planning/phases/15-documentation/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agent definitions and skill cards</name>
  <files>
    .claude/agents/techlead.mdc
    .claude/agents/backend-lead.mdc
    .claude/agents/frontend-lead.mdc
    .claude/agents/db-lead.mdc
    .claude/agents/testlead.mdc
    .claude/agents/i18n-lead.mdc
    .claude/skills/fastapi.mdc
    .claude/skills/postgres.mdc
    .claude/skills/testing-backend.mdc
  </files>
  <action>
**Agent files (.claude/agents/*.mdc):**

In each agent MDC file, find and replace make references:
- `prefers \`make\` commands` → `prefers \`just\` recipes`
- `prefers \`make\` targets` → `prefers \`just\` recipes`
- `\`make frontend\`` → `\`just dev::frontend\``
- `\`make backend\`` → `\`just dev::backend\``
- `\`make dev\`` → `\`just dev::dev\``
- `\`make test-backend\`` → `\`just test::backend\``
- `\`make test-e2e\`` → `\`just test::e2e\``
- `\`make lint\`` → `\`just test::lint\``
- `\`make quality\`` → `\`just test::quality\``
- `make` (in running prose, not backtick-wrapped) where it means "use make" → replace with "just"
- Any other make target references → use the mapping from 15-RESEARCH.md Pattern 1

**Skill files (.claude/skills/*.mdc):**

In each skill MDC file, find and replace:
- `\`make test-backend\`` → `\`just test::backend\``
- `\`make lint\`` → `\`just test::lint\``
- `\`make quality\`` → `\`just test::quality\``
- `make` references in testing/running instructions → just equivalents
- Any other make target references per the standard mapping

**Mapping reference (same as Plan 01 Task 2):**
- `make setup` → `just setup`
- `make dev` → `just dev::dev`
- `make backend` → `just dev::backend`
- `make frontend` → `just dev::frontend`
- `make build-frontend` → `just dev::build-frontend`
- `make test-backend` → `just test::backend`
- `make test-all` → `just test::all`
- `make test-e2e` → `just test::e2e`
- `make lint` → `just test::lint`
- `make quality` → `just test::quality`
- `make db-init` → `just db::init`
- `make db-seed` → `just db::seed`
- `make db-reset` → `just db::reset`
- `make db-migrate` → `just db::migrate`
- `make db-upgrade` → `just db::upgrade`
- `make docker-build` → `just docker::build`
- `make docker-up` → `just docker::up`
- `make docker-down` → `just docker::down`
- `make help` → `just` (bare)
- `make translate-upload` → `just i18n::upload`
- `make translate-download` → `just i18n::download`
- `make translate-status` → `just i18n::status`
- `make translate-pseudo` → `just i18n::pseudo`

**Clean break posture:** No mention of make. Write as if just was always the tool.
  </action>
  <verify>
    <automated>cd /Users/joe/Code/github.com/poindexter12/maxwells-wallet && ! git grep -i '`make' -- '.claude/agents/' '.claude/skills/'</automated>
  </verify>
  <done>All 6 agent MDC files and 3 skill MDC files reference just recipes instead of make targets. Zero backtick-wrapped make commands remain in .claude/ directory.</done>
</task>

<task type="auto">
  <name>Task 2: Update all remaining docs and run DOC-03 grep audit</name>
  <files>
    CONTRIBUTING.md
    OPENSSF.md
    docs/MAKEFILE.md
    docs/i18n-workflow.md
    docs/installation.md
    docs/requirements/README.md
    docs/requirements/TECHNICAL_SPECIFICATIONS.md
    docs/tech-debt/README.md
    docs/tech-debt/backend.md
    docs/tech-debt/database.md
    docs/tech-debt/testing.md
    docs-site/developer/architecture.md
    docs-site/developer/database.md
    docs-site/developer/testing.md
    docs-site/getting-started/development.md
    docs-site/getting-started/quickstart.md
    docs-site/index.md
    frontend/e2e/README.md
    frontend/src/messages/CLAUDE.md
    backend/README.md
    data/CLAUDE.md
    deploy/swag-test/README.md
    .claude/scratchpad/sqlalchemy-migration-plan.md
  </files>
  <action>
**docs/MAKEFILE.md — FULL REWRITE:**
- Rename the file content to reference just recipes (the filename stays since Phase 16 handles file deletion/rename)
- Actually: rewrite as "Just Recipe Reference" document. Change the title from "# Makefile Commands" to "# Just Recipes". Rewrite all sections to use just recipe syntax. Remove "File Organization" section referencing `make/*.mk` and replace with `.just/*.just` module organization.
- Use the same module-organized structure as CLAUDE.md Development Commands section
- This is the most comprehensive reference doc — include ALL recipes from all modules

**docs/i18n-workflow.md:**
- Replace "## Make Commands" heading with "## Just Commands"
- Replace command table entries:
  - `make translate-upload` → `just i18n::upload`
  - `make translate-download` → `just i18n::download`
  - `make translate-status` → `just i18n::status`
  - `make translate-pseudo` → `just i18n::pseudo`
  - `make translate-harvest-new` → `just i18n::harvest-new`
  - Any other make references

**docs/installation.md:**
- Replace make commands with just equivalents
- Add mise prerequisite if setup instructions exist

**frontend/e2e/README.md:**
- Replace `make test-e2e` → `just test::e2e`
- Replace `make test-e2e-headed` → `just test::e2e-headed`
- Replace `make dev` → `just dev::dev`
- Keep `npx playwright test` as direct alternative for frontend-only invocation (it's a different context — running from frontend/ dir)

**frontend/src/messages/CLAUDE.md:**
- Replace any make references with just equivalents

**backend/README.md:**
- Replace make references with just equivalents

**data/CLAUDE.md:**
- Replace make references with just equivalents (data-* recipes delegate to `just utils::data-*`)

**deploy/swag-test/README.md:**
- Replace make references with just equivalents

**docs-site/ files (all 6):**
- Replace all make commands with just equivalents
- Add mise prerequisite where setup instructions appear
- Keep consistent with README.md approach (concise, mise-first)

**docs/requirements/ and docs/tech-debt/ files:**
- Replace make references with just equivalents
- These are internal docs — straightforward find/replace

**CONTRIBUTING.md:**
- Replace make commands with just equivalents
- Add mise prerequisite if setup instructions exist

**OPENSSF.md:**
- Replace make references if present

**.claude/scratchpad/sqlalchemy-migration-plan.md:**
- Replace `make test-backend` → `just test::backend`

**CHANGELOG.md — DO NOT TOUCH:**
- Historical record. Past release notes describing what was shipped are not instructional documentation. Leave as-is.

**Final DOC-03 audit:**
After all edits, run:
```bash
git grep -i '`make' -- ':!Makefile' ':!make/' ':!.planning/' ':!CHANGELOG.md'
```
This MUST return zero results. If any remain, fix them.

Note on CHANGELOG.md exclusion: The user's DOC-03 grep audit (`git grep -i "\`make"`) in the success criteria excludes Makefile and make/ directory. CHANGELOG.md contains historical release notes, not instructional documentation. The clean break posture applies to docs that tell users what to do, not historical logs of what was done.
  </action>
  <verify>
    <automated>cd /Users/joe/Code/github.com/poindexter12/maxwells-wallet && test -z "$(git grep -i '`make' -- ':!Makefile' ':!make/' ':!.planning/' ':!CHANGELOG.md')"</automated>
  </verify>
  <done>All documentation files updated. DOC-03 grep audit returns zero results (excluding Makefile, make/, .planning/, CHANGELOG.md). Every instructional make reference in the repo replaced with just equivalent.</done>
</task>

</tasks>

<verification>
Run the definitive DOC-03 audit:
```bash
# Primary audit (MUST return empty)
git grep -i '`make' -- ':!Makefile' ':!make/' ':!.planning/' ':!CHANGELOG.md'

# Broader check for non-backtick make references in docs
git grep -i '\bmake\b' -- ':!Makefile' ':!make/' ':!.planning/' ':!CHANGELOG.md' '*.md' '*.mdc' | grep -v 'mise\|Makefile\|makefile\|make sure\|make a\|make it\|make the\|make this\|make sense\|make changes\|make any\|make decisions' | head -20

# Verify just references exist in key files
grep -l 'just ' .claude/agents/*.mdc | wc -l   # Should be 6
grep -l 'just ' docs/i18n-workflow.md            # Should match
grep -l 'just test::e2e' frontend/e2e/README.md  # Should match
```
</verification>

<success_criteria>
- `git grep -i '`make'` returns zero results (excluding Makefile, make/, .planning/, CHANGELOG.md)
- All 6 agent MDC files reference just recipes
- All 3 skill MDC files with make references updated
- docs/MAKEFILE.md rewritten as just recipe reference
- docs/i18n-workflow.md uses just i18n:: commands
- frontend/e2e/README.md uses just test:: commands
- All docs-site/ files use just commands
- All other scattered docs updated
- CHANGELOG.md left untouched (historical record)
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation/15-02-SUMMARY.md`
</output>
